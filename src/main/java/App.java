import java.util.Random;

public class App {
//-Random num generator for risky or safe
private static BoundedPriorityQueueSet queue;
private static final Random random = new Random();

    public static void main(String[] args) {
        Boolean riskyOrSafe = random.nextBoolean();

        //Menu switch cases here.

        //-ask user size to make queue
        //-repeat offer menu
        //-add new task
        //-view next task
        //-if queue empty after this then congrats
        //-view num of tasks remaining
        //-view space remaining in queue
        //-exit program
    }

//-after exit, write remaining tasks to a file
//-ask user was it risky or safe
//-congrats if correct console is wrong.
}
